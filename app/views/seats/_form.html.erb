<%= form_for(@seat, url: createfirstseat_path(:flight_id => @flight.id, :class_id => @first.id)) do |form| %>

  <% if seat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seat.errors.count, "error") %> prohibited this seat from being saved:</h2>

      <ul>
      <% seat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
      <%= form.label :class_id %>
      <%= form.number_field :class_id %>
    </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
