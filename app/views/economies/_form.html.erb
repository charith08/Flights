<%= form_for Economy.new, url: flight_economies_path do |form| %>
  <% if economy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(economy.errors.count, "error") %> prohibited this economy from being saved:</h2>

      <ul>
      <% economy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :row %>
    <%= form.number_field :row %>
  </div>

  <div class="field">
    <%= form.label :seats_in_row %>
    <%= form.number_field :seats_in_row %>
  </div>

  <%= form.hidden_field :economy_price, value:  @flight.base_price  %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
