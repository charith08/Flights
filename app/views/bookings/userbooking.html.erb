<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Pnr</th>
      <th>Seats</th>
      <th colspan="3"></th>
    </tr>
  </thead>

<% puts 'aaaaaaaaa' %>

<tbody>
<% @bookings.each do |booking| %>
  <tr>

    <td><%= booking.name %></td>
    <td><%= booking.email %></td>
    <td><%= booking.pnr %></td>
    <td><%= booking.seats %></td>
    <td><%= link_to 'Book Seats', '#' %> </td>
  </tr>
<% end %>
</tbody>
</table>
