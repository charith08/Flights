

<%= form_for First.new, url: flight_firsts_path do |form| %>

<% if first.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(first.errors.count, "error") %> prohibited this first from being saved:</h2>

    <ul>
    <% first.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

  <div class="field">
    <%= form.label :row %>
    <%= form.number_field :row %>
  </div>

  <div class="field">
    <%= form.label :seats_in_row %>
    <%= form.number_field :seats_in_row %>
  </div>

 <%= form.hidden_field :first_price, value: (@flight.base_price * 0.2) + @flight.base_price  %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
